{
  "name": "aztec-wormhole-zkpassport",
  "version": "1.0.0",
  "description": "Monorepo for Aztec/Wormhole demo with frontend, Foundry, Go relayer, and Noir",
  "private": true,
  "main": "index.js",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "npm run build:contracts && turbo run build && npm run copy:artifacts",
    "build:contracts": "cd packages/aztec-contracts && npm run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "export:token": "node ./scripts/export-token-artifact.mjs",
    "copy:artifacts": "npm run export:token && node ./scripts/copy-artifacts.mjs"
  },
  "workspaces": [
    "packages/*",
    "packages/*/*"
  ],
  "devDependencies": {
    "turbo": "^2"
  },
  "packageManager": "npm@10.9.0"
}