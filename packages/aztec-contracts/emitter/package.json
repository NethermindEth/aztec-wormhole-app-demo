{
  "name": "aztec-emitter",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "aztec-nargo compile && aztec-postprocess-contract",
    "copy:artifacts": "node ../../../scripts/copy-artifacts.mjs --only=emitter",
    "postbuild": "npm run copy:artifacts",
    "clean": "rm -rf target"
  }
}

